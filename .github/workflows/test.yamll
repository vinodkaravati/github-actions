name: Deploy App

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment: DEV   

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Use Secrets
        run: |
          echo "API_KEY is ${{ secrets.NAME }}"
          
